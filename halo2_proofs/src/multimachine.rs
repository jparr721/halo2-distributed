//! An interface for dealing with the kinds of distributed computations involved in
//! `halo2`.

/// Returns the number of worker machines that are running.
pub fn current_num_machines() -> usize {
    0
}
